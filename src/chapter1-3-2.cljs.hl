(page "chapter1-3-2.html"
  (:require
   [app.elements :refer [a-item]]))

(html
  (head
    (link :rel "stylesheet" :href "app.css"))
  (body
    (h1 "Exercise 1.34")
    (ul
      (a-item "(defn f [g] (g 2))")
      (a-item "(f f) ;=> (2 2)"))
    ))
